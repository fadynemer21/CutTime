{
  "name": "cutime-functions",
  "version": "1.0.0",
  "private": true,
  "description": "Trusted notification producers for the CuTime appointment lifecycle.",
  "main": "lib/index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "test": "npm run build && node --test lib/test/**/*.test.js",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "firebase-admin": "14.2.0",
    "firebase-functions": "7.3.0"
  },
  "devDependencies": {
    "@types/node": "24.10.0",
    "typescript": "5.9.3"
  }
}
